# Ignore Jupyter notebooks in language statistics otherwise the repo language is Jupyter notebook (as the files take up the most space)
*.ipynb linguist-detectable=false
*.yml linguist-detectable=true
